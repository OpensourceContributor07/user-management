<% for(var i=0;i<users.length;i++){ %>
<tr>
    
    <td><%= users[i].firstname %></td>
    <td><%= users[i].lastname %></td>
    <td><%= users[i].email %></td>
    <td><%= users[i].mobile %></td>
    <td><%= users[i].hobby %></td>
    <td><%= users[i].gender %></td>

    <td>
      <a href="/update-user?id=<%= users[i]._id%>" class="btn-border-shadow update"><span class="text-gradiant"><i class="fas fa-pencil-alt"></i></span></a>
      <a class="btn-border-shadow delete" data-id= <%= users[i]._id%> >  <span class="text-gradiant"><i class="fas fa-times"></i></span></a>

    </td>
  </tr>

  <%}%>